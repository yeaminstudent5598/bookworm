import ResetPasswordClient from "./component/ResetPasswordClient";

export const metadata = {
  title: "Reset Password | BookWorm",
};

export default function ResetPasswordPage() {
  return <ResetPasswordClient />;
}